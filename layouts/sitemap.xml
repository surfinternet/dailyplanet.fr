{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

  {{- /* Homepage — priority max, mise à jour à chaque article */}}
  <url>
    <loc>{{ .Site.BaseURL }}</loc>
    <lastmod>{{ now.Format "2006-01-02" }}</lastmod>
    <changefreq>daily</changefreq>
    <priority>1.0</priority>
  </url>

  {{- /* Liste des articles /posts/ */}}
  {{- range where .Site.Pages "Kind" "section" }}
  {{- if eq .Section "posts" }}
  <url>
    <loc>{{ .Permalink }}</loc>
    <lastmod>{{ .Lastmod.Format "2006-01-02" }}</lastmod>
    <changefreq>daily</changefreq>
    <priority>0.9</priority>
  </url>
  {{- end }}
  {{- end }}

  {{- /* Articles — priorité haute, changefreq weekly (contenu d'actualité) */}}
  {{- range where .Site.RegularPages "Section" "posts" }}
  {{- if not .Draft }}
  <url>
    <loc>{{ .Permalink }}</loc>
    <lastmod>{{ .Lastmod.Format "2006-01-02" }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.8</priority>
  </url>
  {{- end }}
  {{- end }}

  {{- /* Pages statiques (about, contact, legal, charter, auteurs) */}}
  {{- range where .Site.RegularPages "Section" "!=" "posts" }}
  {{- if not .Draft }}
  <url>
    <loc>{{ .Permalink }}</loc>
    <lastmod>{{ .Lastmod.Format "2006-01-02" }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.5</priority>
  </url>
  {{- end }}
  {{- end }}

</urlset>
